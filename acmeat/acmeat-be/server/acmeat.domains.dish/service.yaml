apiVersion: v1
kind: Service
metadata:
  name: dishmanager-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: dishmanager
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
