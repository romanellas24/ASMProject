apiVersion: v1
kind: Service
metadata:
  name: localmanager-service
  namespace: acmeat
spec:
  type: ClusterIP
  selector:
    app: localmanager
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
